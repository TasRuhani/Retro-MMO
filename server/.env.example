# PostgreSQL Database Connection
# For localhost development (PostgreSQL 17 on port 5432)
DATABASE_URL=postgresql://postgres:password@localhost:5432/Retro

# For AWS RDS production (uncomment and update when ready)
# DATABASE_URL=postgresql://username:password@your-rds-endpoint.amazonaws.com:5432/retrommo

# Session Configuration
SESSION_TIMEOUT_MS=120000
HEARTBEAT_INTERVAL_MS=30000

# Server Configuration
PORT=3000
NODE_ENV=development
